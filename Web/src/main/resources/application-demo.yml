# Demo-profiel: een lokale speeltuin om de applicatie met de hand uit te proberen.
#
# WAARSCHUWING. Dit profiel zet de setup-API aan (catalogimport.setup-api.enabled). Die API kent geen
# authenticatie of autorisatie (Fase 5) en kan importdefinities en hun drempels bepalen. Gebruik dit
# profiel uitsluitend op een ontwikkelmachine met wegwerpgegevens, nooit in productie.
#
# Start: mvn -pl Web -am spring-boot:run -Dspring-boot.run.profiles=local,demo
# Bij het opstarten maakt DemoDataSeeder een volledige voorbeeldketen en logt hij de taskId.
spring:
  # Dezelfde in-memory H2 als application-local.yml, zodat het profiel ook alleen gebruikt kan
  # worden (-Dspring-boot.run.profiles=demo). Met local,demo zijn beide waarden identiek.
  datasource:
    url: jdbc:h2:mem:catalogimport;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console
catalogimport:
  setup-api:
    # Alleen hier true. application.yml laat de vlag bewust weg, zodat elke andere omgeving uit staat.
    enabled: true
  archive:
    # Eigen archiefmap, zodat een demoproef nooit in de map van het local-profiel schrijft.
    root: ${java.io.tmpdir}/catalogimport-demo-archive
