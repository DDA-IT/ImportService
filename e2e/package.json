{
  "name": "catalogimport-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end tests voor CatalogImport (echte frontend, echte backend, echte PostgreSQL)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
